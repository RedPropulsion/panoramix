description: Waveshare ST3215 serial bus servo motor

compatible: "waveshare,st3215"

include: base.yaml

properties:
  gpios:
    type: phandle-array
    required: true
    description: GPIO pin to control TX/RX direction (Enable pin)